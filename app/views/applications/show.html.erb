<h1><%= "#{@application.name}'s Application" %></h1>

<p>Name: <%= @application.name %></p>
<p>Address: <%= "#{@application.street}, " "#{@application.city}, " "#{@application.state} " "#{@application.zip}" %></p>
<p>Why would you make a good home for the pet(s): <%= @application.description %></p>
<p>Pet Names:
  <% @application.pets.each do |pet| %>
    <%= link_to "#{pet.name}", "/pets/#{pet.id}" %>
  <% end %>
</p>
<p>Application Status: <%= @application.status %></p>
<p>
  <%= form_with url:"/applications/#{@application.id}?search=pet_name", method: :get, local: true do |form| %>
    <%= form.label :pet_name, "Pet Name: " %>
    <%= form.text_field :pet_name %>
    <%= form.submit "Search" %>
  <% end %>
</p>
<p>
  <% @pets.each do |pet| %>
    <p><%= pet.name %></p>
  <% end %>
</p>
